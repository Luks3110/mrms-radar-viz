# Backend URL for proxying API requests during development
VITE_BACKEND_URL=http://localhost:8000

# API base URL for frontend (use /api for proxy, or full URL for direct connection)
VITE_API_BASE_URL=/api
